#!/usr/bin/make -f

DEB_MAKE_INSTALL_TARGET := install DESTDIR=$(CURDIR)/debian/udr/usr

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/makefile.mk


# Add here any variable or target overrides you need.
